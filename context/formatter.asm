ror al,8
jmp [esi]
test ebx,93
rol bx,4
dec ebp
wait
jmp [edi]
lock
ror esi,2
test bh,59
pause
cmp edi,esi
cld
nop
ret
jmp [ebx]
ror rbp,4
idiv al
rcl ebx,7
jmp [edi]
ror rax,8
scasb
rcr dx,2
jmp [esi]
scasw
sub bl,bl
xor dh,64
rcr edx,6

pause
div ecx
or [rdx],rsi
lock
sal ebp,3
cli
sar dh,6
shr cx,3
neg rdi
or [esi],ebx
hlt
shr cl,7
call [esi]
imul rbx
jmp [eax]
rol rbx,8
mul ax
wait
or dh,dh
jmp [ecx]
idiv cx
idiv ax
mov [ecx],ebx
adc [ebx],ebx
dec ax
sbb [rsi],rbx
inc rbp
rcl eax,7
jmp [ebp]
call [ebx]
rol rdi,8
cli
jmp [esi]
sar edi,8
stc
sal rdi,3
hlt
neg ebp
rol ebx,3
sbb [rbp],rdx
jmp [edx]
lodsw
jmp [esi]
idiv bh
ret
sti

lock
wait
test dx,dx
.Ut221195490:
call [edx]
call [ecx]
sar esi,4
sub rcx,rax
mul rcx
rcl edx,1
inc ch
rol bl,2
call [ebx]
add edx,5
inc ch
idiv ah
lodsw
lodsb
dec rdi
jmp [eax]
sbb [rdi],rbx
inc cl
sal al,4
mul edx
scasw
ret
shr dx,6
lock
cmpsw
lahf
neg edx
test rcx,81
rol ah,1
sal ah,7
cmp ch,ch
ret
cli
lodsb

cmp [rdi],rcx
jmp [ecx]
rdtsc
jmp [eax]
inc edx
xor cx,73
and ah,74
rcr al,2
rcr eax,3
rol rsi,4
std
lock
scasb
ret
shr rdi,1
rcl rbx,5
sbb ch,73
shl bl,6
sal cl,6
call [ebp]
or bx,8
mov [rsi],rdi
pause
neg rax
add eax,31
pause
neg ax
shr al,8
add rcx,72
rcr esi,2
lodsw
syscall
shr ecx,7
cmp ch,ch
call [eax]
rol cx,2
sbb [rcx],rax
rol ax,1
idiv bh
jmp [edi]
rcr cx,6
ror rdi,3
adc [edx],ebx
wait
mul ah
lodsb
rcr edx,8
lodsb
rol bx,8
cwd
pause
mul dl
ret
call [esi]
sub dx,dx
jmp [ebx]
rdtsc
idiv dh
rol ebp,6
rol ah,3
and eax,8
rol ah,7
sbb rax,rbp
ror cx,5
mov eax,53
shr bx,7
sar dx,6
shr eax,2
imul ebp
add [rsi],rbp
imul ebp
call [eax]
ret
jmp [eax]
mul rdx
sbb [ebp],eax
or al,36
rol esi,7
mov rdx,rbp
.Om406905359:
nop
adc cx,cx
imul rdx
sbb [rsi],rbp
neg dh
cli
.y4f572505756:
jmp [esi]
add dh,25
jmp [ebx]
lodsb
sar ebp,2
stc
sbb [eax],esi
jmp [eax]
jmp [edx]
or bx,77
lahf
ror rax,1
sal bx,7
adc dl,40
clc
scasw
sar rsi,5
sub [rdx],rbp
sbb rsi,39
mov [eax],ecx
jmp [ecx]
ret
rdtsc
ret
neg bx
test [rsi],rbx
sbb cx,99
rcl edx,1
lodsw
scasb
rol esi,8
mul ax
.AYvfAa359548210:
sti
shr bh,7