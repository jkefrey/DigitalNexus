imul rsi
sbb rax,14
std
xor ax,24
cmp [rdi],rcx
sub bx,bx
hlt
stc
inc rbx
mov [eax],esi
imul rdx
inc rbx
stc
dec al
call [eax]
sub dx,dx
ret
sar ebx,3
ret
or [rdi],rdx
sbb [ebp],ebp
jmp [edi]
clc
adc cx,ax
shr eax,6
rol edx,3
jmp [ecx]
mul esi
neg cl
rol cx,2
jmp [eax]
lodsw
sti
sti
ret
cmp dh,ah
ror dx,2
jmp [edx]
and dx,27
idiv edi
or dl,11
dec rcx

cwd
cwd
sal al,4
jmp [edx]
wait
lahf
rdtsc
rol rax,5
and [rbp],rcx
call [esi]
jmp [edi]
sal cx,6
div cx
lock
sub [rdi],rdi
neg dx
add rcx,59
sar dx,5
cmp ebx,edx
sbb rbp,32
mul dl
lodsb
pause
rol ch,1
pause
clc
lahf
ret
jmp [edx]
test cl,44
call [ecx]
ret

cwd
dec rbx
lodsb
mh1b4U9u548697418:
sbb dh,ch
lodsb
add rdx,rdx
sal rbx,6
pause
add dx,dx
std
imul al
pause
syscall
cmp bl,75
adc edx,29
lock
sal ecx,7
or cl,92
sub cl,cl
rcr eax,8
cwd
ret
call [edx]
mul ecx
call [edx]
rcr ecx,7
sal rbp,5
std
mov dx,48
lahf

jmp [ebp]
stc
inc bh
sar edx,7
nop
syscall
shr bx,5
.NP4ezeS482042777:
scasb
rol dx,1
add rbx,5
cmpsw
inc al
wait
neg rdi
sar rdi,6
wait
rcl cl,5
inc edi
syscall
sar dh,2
mov dh,dh
div rdi
call [eax]
clc
jmp [ebx]
stc
shr rcx,1

hlt
cld
add al,al
test cl,80
and edx,ebx
ror bx,4
rol ah,5
dec cx
clc
syscall
sar rdx,8
nop
call [edi]
adc [ebx],eax
mul dl
ret
nop
or rsi,rbx
jmp [ebx]
sbb rcx,rax
sti
inc dl
scasb
sub [rcx],rsi
ret
sar ebp,3
scasb
sbb cx,70
div ebx
dec cl
syscall
cwd
stc
sbb dl,55
cli
mov edx,10
sal rax,2
lodsb
and cx,cx
test cx,74
pause
cli
sti
idiv eax
shr dx,3
clc
cli
neg rsi
wait
adc bl,52
wait
ror dl,5
cwd
sar rbp,5
rcl rsi,6
imul rbx
adc [rax],rdi
idiv ecx
shl ah,1
jmp [eax]
shl rsi,3
imul dx
ror al,5
rdtsc
call [ebp]
.bLFQU617196105:
call [eax]
test rax,rdx
lodsw
sal bx,7
ret
stc
adc ch,ch
and dx,dx
div ax
add ebx,edx
cmp bl,77
cli
jmp [ebx]
rol ecx,4
cld
add dh,47
test ax,ax
test [ebp],eax
and al,97
dec rbx
rcl ax,3
cmp dx,ax
syscall
ror dx,3
pause
cmp cl,54
rcl dl,1
cmp [edx],edx
d5Bx216387201:
sub dx,dx
neg esi
sbb cx,cx
rcl dh,1
call [edi]
ret
sbb dh,dh
stc
sti
pause
shl cx,7
shl dx,5
scasw
wait
ret
lahf
imul ebx
idiv cl
shr rsi,2
ret
cld
neg rdx
rcl bx,1
scasb
or cl,69
ror rbp,3
nop
jmp [ecx]
mul al
lodsb
syscall
call [ebx]
cmp ax,18
add ah,21
adc [eax],ebp
sal rcx,7
or [rbp],rbx
sbb cx,31
jmp [ebx]
idiv ch
call [edi]
imul ebp
mul dl
jmp [edx]
sal dh,5
mov dh,dh
inc ebx
or ebp,eax
sti
lodsw
xor ebx,16
call [ecx]
dec rax
rcl ch,8
call [ebp]
idiv ebx
shl ebx,1
clc
ror edx,7
hlt
wait
cld
ror ax,5
sub [esi],edx
sub ch,ch
jmp [edx]
rcr ebx,5
sbb eax,ebp
ror bh,4
idiv ebp
jmp [ebx]
test ax,ax
syscall
rdtsc
mul eax
rol esi,4
jmp [esi]

and eax,edi
scasb
ror dh,8
sbb cx,cx
mov ecx,ecx
mov rbx,rdi
imul eax
lodsb
cld
dec edi
cmp [rdi],rcx
and ecx,6
sar esi,4
neg dl
ror al,5
jmp [ebp]
adc rbx,50
cmpsw
ret
inc dl
wait
sal dh,5
ror cx,2
add [edx],ebx
idiv al
cmpsw
xor [ebx],ebx
adc bh,65
add ax,41
adc ax,64
and rsi,rcx
rcl ebx,3
jmp [ecx]
cld
wait
add bl,bl
and eax,ebp
cld
rcl bl,7
sal ebx,4
xor eax,10
sal ch,5
sar dx,4
clc
cli
dec ebx
sal edi,2
cld
test ax,ax
cmp [edi],edi
idiv dh
inc rdx
mov bx,bx
shl bx,5
idiv rbx
stc
sti
rol ebx,7
add [rsi],rcx
inc ebx
lodsw
cmp ax,ax
neg rdx
dec dl
lahf
xor ax,ax
and bl,51
ror eax,4
call [ebx]
nop
jmp [ecx]
jmp [esi]
xor dl,dl
test bl,52
cmp [esi],ebp
jmp [edx]
cli
neg ebx
sal dh,7
jmp [ebp]

ret
hlt
sti
xor esi,ebx
jmp [ebp]
inc bx
div cl
rcr rax,3
xor [rbp],rdx
cmpsw
idiv ax
div eax
sbb cl,25
or dh,92
imul esi
div edi
jmp [esi]
xor [rdx],rbp
and [eax],esi
jmp [eax]
sbb dh,68
mov [rbx],rdi
shl dx,5
inc bl
ret
dec rsi
div ah
call [ebx]
rcr esi,4
sub bx,8
test al,al
jmp [edx]
rcr dl,6
ror dx,2
wait
neg rbx
call [edi]
shl dl,3
lodsw
sub ax,89
scasw
pause
rcr ch,6
and ah,ah
scasw
rcr edi,4
ret
jmp [esi]
jmp [ebx]
cmp dx,63
sbb ebp,26
rcr rcx,1
xor cl,cl
xor cx,73
jmp [ebp]
rol bh,8
xor cx,94
shr cl,5
dec ch
imul dx
test rcx,rax
scasb
rol edi,6
hlt
idiv rcx
ret